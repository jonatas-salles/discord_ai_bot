- name: ask
  description: Return AI generated answer to user.
  options:
    - name: message
      description: The users question.
      type: 3 # string
      required: true

- name: echo
  description: Echo message back to sender.
  options:
    - name: message
      description: The message to echo back.
      type: 3 # string
      required: true